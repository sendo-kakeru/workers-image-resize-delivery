{
  "name": "infrastructure",
  "main": "index.ts",
  "scripts": {
    "up": "dotenv -- pulumi up -y",
    "down": "dotenv -- pulumi destroy -y"
  },
  "devDependencies": {
    "@workers-image-resize-delivery/eslint-config": "workspace:*",
    "@workers-image-resize-delivery/typescript-config": "workspace:*",
    "@types/node": "^18",
    "dotenv-cli": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@pulumi/cloudflare": "^6.2.0",
    "@pulumi/pulumi": "^3.113.0",
    "@workers-image-resize-delivery/common": "workspace:*",
    "dotenv": "^16.5.0"
  },
  "packageManager": "pnpm@9.0.0"
}
